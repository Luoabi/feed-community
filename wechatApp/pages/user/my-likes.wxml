<view class="container">
  <view class="likes-list">
    <view 
      class="like-item" 
      wx:for="{{likes}}" 
      wx:key="id"
      bindtap="onItemTap"
      data-id="{{item.contentId || item.id}}"
      data-type="{{item.contentType || 'post'}}"
    >
      <view class="item-header">
        <image class="author-avatar" src="{{item.authorAvatar || '/images/default-avatar.svg'}}"></image>
        <view class="author-info">
          <text class="author-name">{{item.authorName}}</text>
          <text class="like-time">{{item.createTime}}</text>
        </view>
      </view>
      
      <view class="item-content">
        <view class="content-text">
          <text class="content-title" wx:if="{{item.title}}">{{item.title}}</text>
          <text class="content-desc">{{item.content}}</text>
        </view>
        <image 
          wx:if="{{item.coverImage || (item.images && item.images[0])}}"
          class="content-cover" 
          src="{{item.coverImage || item.images[0]}}" 
          mode="aspectFill"
        />
      </view>
      
      <view class="item-stats">
        <text class="stat-item">❤️ {{item.likes || 0}}</text>
        <text class="stat-item">💬 {{item.comments || 0}}</text>
      </view>
    </view>
  </view>

  <view class="empty-tip" wx:if="{{!loading && likes.length === 0}}">
    <text>暂无点赞记录</text>
  </view>

  <view class="loading-tip" wx:if="{{loading}}">
    <text>加载中...</text>
  </view>
</view>
